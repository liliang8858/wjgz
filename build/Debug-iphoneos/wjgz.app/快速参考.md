# 🎵 音效系统快速参考

## 📊 统计信息
- **总音效数**: 27 个
- **背景音乐**: 3 个 (7.0MB)
- **游戏音效**: 24 个 (4.5MB)
- **总大小**: 11.5MB
- **格式**: MP3 + WAV

## 🎮 常用 API

### 背景音乐
```swift
// 播放主背景音乐
SoundManager.shared.playBackgroundMusic("background_main")

// 播放史诗音乐
SoundManager.shared.playBackgroundMusic("background_epic")

// 停止音乐
SoundManager.shared.stopBackgroundMusic()

// 淡出音乐
SoundManager.shared.fadeOutBackgroundMusic(duration: 1.0)

// 设置音量
SoundManager.shared.setMusicVolume(0.4)  // 0.0 - 1.0
```

### 基础操作
```swift
SoundManager.shared.playTap()      // 点击
SoundManager.shared.playSelect()   // 选中剑
SoundManager.shared.playDrag()     // 拖拽剑
SoundManager.shared.playDrop()     // 放下剑
```

### 合成音效
```swift
SoundManager.shared.playMergeFan()   // 凡剑 ⭐⭐
SoundManager.shared.playMergeLing()  // 灵剑 ⭐⭐⭐
SoundManager.shared.playMergeXian()  // 仙剑 ⭐⭐⭐⭐
SoundManager.shared.playMergeShen()  // 神剑 ⭐⭐⭐⭐⭐
```

### 特效音效
```swift
SoundManager.shared.playCombo(count)      // 连击 (自动根据数量)
SoundManager.shared.playChainClear()      // 连锁消除
SoundManager.shared.playExplosion()       // 区域爆炸
SoundManager.shared.playLevelUp()         // 升级光柱
SoundManager.shared.playUltimate()        // 万剑归宗
```

### UI音效
```swift
SoundManager.shared.playButton()          // 按钮点击
SoundManager.shared.playEnergyFull()      // 能量满
SoundManager.shared.playLevelComplete()   // 关卡完成
SoundManager.shared.playGameOver()        // 游戏结束
SoundManager.shared.playStar()            // 星星
SoundManager.shared.playError()           // 错误
```

### 音量控制
```swift
// 设置音效音量
SoundManager.shared.setSFXVolume(0.7)  // 0.0 - 1.0

// 启用/禁用音效
SoundManager.shared.setEnabled(true)
```

## 🎯 典型使用场景

### 游戏启动
```swift
override func didMove(to view: SKView) {
    SoundManager.shared.setMusicVolume(0.4)
    SoundManager.shared.setSFXVolume(0.7)
    SoundManager.shared.playBackgroundMusic("background_main")
}
```

### 剑的拖拽
```swift
// 开始拖拽
func touchesBegan() {
    SoundManager.shared.playSelect()
}

// 拖拽中
func touchesMoved() {
    SoundManager.shared.playDrag()
}

// 结束拖拽
func touchesEnded() {
    SoundManager.shared.playDrop()
}
```

### 剑的合成
```swift
func mergeSwords(type: SwordType) {
    // 播放合成音效
    switch type {
    case .fan: SoundManager.shared.playMergeFan()
    case .ling: SoundManager.shared.playMergeLing()
    case .xian: SoundManager.shared.playMergeXian()
    case .shen: SoundManager.shared.playMergeShen()
    }
    
    // 播放特效
    effectsManager.playMergeBurst(at: position, color: type.color, swordType: type)
}
```

### 连击系统
```swift
func updateCombo() {
    comboCount += 1
    
    // 播放连击音效（自动根据数量选择）
    SoundManager.shared.playCombo(comboCount)
    
    // 播放连击特效
    effectsManager.playComboEffect(combo: comboCount, at: position)
}
```

### 万剑归宗
```swift
func activateUltimate() {
    // 播放终极技音效（完整序列）
    SoundManager.shared.playUltimate()
    
    // 播放终极技特效
    effectsManager.playUltimateEffect()
}
```

## 📁 文件结构速查

```
Sounds/
├── BGM/
│   ├── background_main.mp3      # 主游戏音乐
│   ├── background_epic.mp3      # 史诗音乐
│   └── background_menu.mp3      # 菜单音乐
│
└── SFX/
    ├── Sword/
    │   ├── sword_whoosh.wav     # 挥剑
    │   ├── sword_clash.mp3      # 碰撞
    │   ├── sword_draw.mp3       # 拔剑
    │   └── sword_sheath.mp3     # 收剑
    │
    ├── Merge/
    │   ├── merge_small.mp3      # 凡剑
    │   ├── merge_medium.mp3     # 灵剑
    │   ├── merge_large.mp3      # 仙剑
    │   └── merge_epic.mp3       # 神剑
    │
    ├── Effects/
    │   ├── combo.wav            # 连击
    │   ├── combo_low.mp3        # 低连击
    │   ├── combo_high.mp3       # 高连击
    │   ├── explosion.wav        # 爆炸
    │   ├── power_up.mp3         # 能量
    │   ├── success.mp3          # 成功
    │   ├── whoosh.mp3           # 快速移动
    │   ├── sparkle.mp3          # 闪光
    │   └── error.mp3            # 错误
    │
    ├── UI/
    │   ├── button_click.mp3     # 按钮
    │   ├── level_complete.mp3   # 完成
    │   ├── game_over.mp3        # 结束
    │   └── star_collect.mp3     # 星星
    │
    └── Ultimate/
        ├── ultimate_charge.mp3  # 蓄力
        ├── ultimate_release.wav # 释放
        └── ultimate_impact.wav  # 冲击
```

## ⚡ 性能优化

### 预加载
```swift
// SoundManager 会自动预加载常用音效
// 无需手动操作
```

### 并发播放
```swift
// 支持同时播放多个相同音效
// 每个音效最多3个实例
// 自动管理，无需手动控制
```

### 内存管理
```swift
// 播放器池自动管理
// 不播放时自动回收
// 无内存泄漏风险
```

## 🐛 故障排除

### 听不到音效？
1. 检查设备音量
2. 检查 `SoundManager.shared.setEnabled(true)`
3. 检查文件是否在 "Copy Bundle Resources"
4. 查看控制台日志

### 音效延迟？
1. 确保音效已预加载
2. 使用 MP3 而不是 WAV（更小）
3. 检查音效文件大小（建议 < 100KB）

### 背景音乐不循环？
```swift
// 确保使用 loop: true
SoundManager.shared.playBackgroundMusic("background_main", loop: true)
```

## 📝 检查清单

在 Xcode 中：
- [ ] Sounds 文件夹已添加到项目（蓝色文件夹）
- [ ] 所有音效在 "Copy Bundle Resources" 中
- [ ] 运行游戏，背景音乐自动播放
- [ ] 点击剑有音效
- [ ] 合成剑有音效
- [ ] 连击有音效

## 🎉 享受游戏！

所有音效已配置完成，现在每个动作都有即时的声音反馈！

配合华丽的特效和触觉震动，将产生源源不断的多巴胺！🎮✨
